{
  "name": "user-explorer-viewer",
  "version": "0.0.0",
  "private": true,
  "description": "Pure analytics helpers for the User Explorer HTML viewer (unit-tested with node --test).",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
